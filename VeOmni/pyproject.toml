[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "VeOmni"
version = "0.1.0"
description = "VeOmni sub-package"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["veomni"]